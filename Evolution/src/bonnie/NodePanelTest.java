package bonnie;

import static org.junit.Assert.*;

import java.awt.Color;

import org.junit.Test;

public class NodePanelTest {

	@Test
	public void test() {
		/*Grid g = new Grid(1, 1);
		Node node = g.getValue(0, 0);
		NodePanel panel = new NodePanel(node);
		panel.update();
		assertEquals(Color.black, panel.getBackground());
		node.setLife(true);
		panel.update();
		assertEquals(Color.red, panel.getBackground());*/
	}

}
